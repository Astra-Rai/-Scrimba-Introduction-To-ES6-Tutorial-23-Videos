//example 1

/*fi(false) {
    var example = 5;
  //if let is used instead of var, returns undefined
}
console.log(example);//null
//let is a stricter version of var, uses block scope
//var using hoisting
//let, const no hoisting
*/

//primitiave data type, number, string, boolean
const example =5;
//example = 10//const, when dealing with primitative types, are read-only
example = 10;
console.log(example);

//when dealing with arrays, we can push values
//you can modify objects and arrays, but you cannot override the datatypes.